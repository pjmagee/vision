@inject IJSRuntime Runtime
@inject SvgHelper SvgHelper

<table class="table table-striped table-bordered table-sm" style="width: 100%">
    <thead>
        <tr>
            <th></th>
            <th>Build</th>
            <th>Web Url</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Items)
        {
            <tr>
                <td class="@item.Kind.GetIconClass()">
                    @SvgHelper.GetSvg(item.Kind)
                </td>
                <td>@item.Name</td>
                <td>
                    <a href="@item.WebUrl" target="_blank">@item.WebUrl (Open in new window)</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@functions
{
    [Parameter] private CiCdBuildDto[] Items { get; set; }

    protected override async Task OnAfterRenderAsync()
    {
        await Runtime.InvokeAsync<object>("dataTable");
    }
}

version: '3.4'

services:
  web:
    build:
      context: .
      dockerfile: Vision.Web/Dockerfile
    depends_on: 
      - db
  db:
    image: mcr.microsoft.com/mssql/server:2017-latest
    ports:
      - 1433:1433
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "P4ssw0rd1!"
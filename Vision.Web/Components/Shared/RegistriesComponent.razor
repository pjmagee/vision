<table class="table table-striped table-bordered table-sm" style="width: 100%">
    <thead>
        <tr>
            <th>Kind</th>
            <th>Endpoint</th>
            <th>Api Key</th>
            <th>Dependencies</th>
            <th>Is Public</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Items)
        {
        <tr>
            <td class="@item.Kind.GetIconClass()">@SvgService.GetSvg(item.Kind)</td>
            <td><a href="@($"/data/registries/{item.RegistryId}")">@item.Endpoint</a></td>
            <td>@item.ApiKey</td>
            <td>@item.Dependencies</td>
            <td>@item.IsPublic</td>
            <td></td>
        </tr>
        }
    </tbody>
</table>

@functions
{
    [Parameter] IEnumerable<RegistryDto> Items { get; set; }

    [Inject] IJSRuntime JSRunTime { get; set; }
    [Inject] SvgService SvgService { get; set; }

    protected override async Task OnAfterRenderAsync()
    {
        await JSRunTime.InvokeAsync<object>("dataTable");
    }
}

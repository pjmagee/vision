<table class="table table-striped table-bordered table-sm" style="width: 100%">
    <thead>
        <tr>
            <th>Kind</th>
            <th>Name</th>
            <th>Assets</th>
            <th>Versions</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Items)
        {
            <tr>
                <td class="@item.Kind.GetIconClass()">@SvgService.GetSvg(item.Kind)</td>
                <td><a href="@($"/data/dependencies/{item.DependencyId}")">@item.Name</a></td>
                <td>@item.Assets</td>
                <td>@item.Versions</td>
                <td></td>
            </tr>
        }
    </tbody>
</table>

@functions 
{
    [Parameter] IEnumerable<DependencyDto> Items { get; set; }

    [Inject] IJSRuntime JSRuntime { get; set; }
    [Inject] SvgService SvgService { get; set; }

    protected override async Task OnAfterRenderAsync()
    {
        await JSRuntime.InvokeAsync<object>("dataTable");
    }
}
